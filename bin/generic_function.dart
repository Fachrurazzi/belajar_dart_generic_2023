import 'helper/array_helper.dart';

void main() {
  var numbers = [1, 2, 3, 4, 5];
  var names = ["Arif", "Anton", "Dayat", "Babe"];

  print(ArrayHelper.count(numbers));
  print(ArrayHelper.count(names));
}
